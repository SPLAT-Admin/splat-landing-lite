import { NextApiRequest, NextApiResponse } from 'next';

export default function handler(req: NextApiRequest, res: NextApiResponse) {
  const baseUrl = 'https://www.usesplat.com';
  const pages = [
    { url: '/', priority: '1.0', changefreq: 'weekly' },
    { url: '/ambassador', priority: '0.9', changefreq: 'monthly' },
    { url: '/ambassador-apply', priority: '0.8', changefreq: 'monthly' },
    { url: '/merch', priority: '0.8', changefreq: 'weekly' },
    { url: '/advertise', priority: '0.7', changefreq: 'monthly' },
    { url: '/contact', priority: '0.6', changefreq: 'monthly' },
    { url: '/signup', priority: '0.9', changefreq: 'monthly' },
    { url: '/login', priority: '0.5', changefreq: 'monthly' },
    { url: '/reset-password', priority: '0.5', changefreq: 'monthly' },
    { url: '/community', priority: '0.7', changefreq: 'monthly' },
    { url: '/checkout', priority: '0.6', changefreq: 'monthly' },
    { url: '/thank-you', priority: '0.6', changefreq: 'monthly' },
    { url: '/status', priority: '0.4', changefreq: 'weekly' },
    { url: '/terms', priority: '0.4', changefreq: 'yearly' },
    { url: '/privacy', priority: '0.4', changefreq: 'yearly' },
    { url: '/sitemap', priority: '0.3', changefreq: 'monthly' }
  ];

  const currentDate = new Date().toISOString().split('T')[0];
  
  const sitemap = `<?xml version="1.0" encoding="UTF-8"?>
  <urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
    ${pages
      .map(
        (page) => `
      <url>
        <loc>${baseUrl}${page.url}</loc>
        <lastmod>${currentDate}</lastmod>
        <changefreq>${page.changefreq}</changefreq>
        <priority>${page.priority}</priority>
      </url>`
      )
      .join('')}
  </urlset>`;

  res.setHeader('Content-Type', 'application/xml');
  res.write(sitemap);
  res.end();
}
